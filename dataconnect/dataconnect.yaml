specVersion: "v1beta"
serviceId: "setanta-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "setanta-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
